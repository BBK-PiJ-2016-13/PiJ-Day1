public class PersonList {
	private int [] list;

	public void add (Person person) {
		int count = 0
		for (x : list, count < list.length, count++) {
			while (person(next) != null) { //look for the last person in list, with no next
			count++;
		}
		 list[count] = person;
		 list[count-1] = person(next);
		 count = list.length;
		}
	}

	public void delete (Person person) {
		int count = 0
		for Person in list {
			if (Person == person) {

			}
		}
	}

	public void print() {
		int count = 0;
		while (count < list.length) {
			System.out.println (list[count]);
			count++;			
			}
	}

}